# Client Environment Variables
# The React dev server will run on this port
PORT=3001

# Cloudflare Worker URL for the API in production builds
# Example: https://personal-yield-api.<your-subdomain>.workers.dev
REACT_APP_API_URL=
